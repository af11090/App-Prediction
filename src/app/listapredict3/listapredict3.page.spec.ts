import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Listapredict3Page } from './listapredict3.page';

describe('Listapredict3Page', () => {
  let component: Listapredict3Page;
  let fixture: ComponentFixture<Listapredict3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Listapredict3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
