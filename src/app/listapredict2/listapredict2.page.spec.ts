import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Listapredict2Page } from './listapredict2.page';

describe('Listapredict2Page', () => {
  let component: Listapredict2Page;
  let fixture: ComponentFixture<Listapredict2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Listapredict2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
